query getLocationDetails($id: ID!) {
    location(id: $id) {
        id
        name
        type
        dimension
        residents {
            id
            name
            species
        }
    }
}

